{
  "name": "request-library-demo",
  "version": "1.0.0",
  "description": "基于三层架构设计的前端请求库，支持axios和fetch两种实现，提供重试、缓存、幂等、并发控制等功能",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "request",
    "axios",
    "fetch",
    "cache",
    "retry",
    "idempotent"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "axios": "^1.13.2"
  },
  "devDependencies": {
    "vite": "^7.3.0"
  },
  "peerDependencies": {
    "axios": "^1.0.0"
  },
  "optionalDependencies": {
    "axios": "^1.0.0"
  }
}
